import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fleetx.fleetapp',
  appName: 'FleetApp',
  webDir: 'dist/FleetApplication/browser'
};

export default config;
